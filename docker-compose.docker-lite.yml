services:
  docker-lite:
    build:
      context: .
      dockerfile: Dockerfile.docker-lite
      network: host
      args:
        LOCAL_USER: ${LOCAL_USER:-devuser}
        LOCAL_UID: ${LOCAL_UID:-1000}
        LOCAL_GID: ${LOCAL_GID:-1000}
        NODE_VERSION: ${NODE_VERSION:-24.14.1}
        DEV_SECRETS_ARCHIVE_B64: ${DEV_SECRETS_ARCHIVE_B64:-}
        DOCKER_LITE_TEMPLATE_SOURCE: ${DOCKER_LITE_TEMPLATE_SOURCE:-docker-lite-template}
    image: panjd123/docker-lite:${CUDA_ENV_IMAGE_VERSION:-v1}
    container_name: docker-lite-dev
    working_dir: /workspace
    tty: true
    stdin_open: true
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ${CUDA_ENV_WORKSPACE_DIR}:/workspace
      - .:/workspace/cuda-env
    ports:
      - "22848:22"

networks:
  default:
    driver: bridge
